<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Tyler Peek | Home</title>
  <link href="/static/style.css" rel="stylesheet">
  <link href='http://fonts.googleapis.com/css?family=Homemade+Apple' rel='stylesheet' type='text/css'>
</head>
<body>
  <header>
    <a href="{{ request.route_url('home') }}" id="logo">
      <h1>Tyler Peek</h1>
      <h2>Python Developer</h2>
    </a>
    <nav>
      <ul>
        {% if request.authenticated_userid %}
        <li><a id="add" href="{{ request.route_url('add') }}">Add</a></li>
        <li><a id="logout" href="{{ request.route_url('logout') }}">Log Out</a></li>
        {% else %}
        <li><a id="login" href="{{ request.route_url('login') }}">Login</a></li>
        {% endif %}
      </ul>
    </nav>
  </header>
  <h3>Journal Entries</h3>
  <div id="entry_field">
  {% for entry in entries %}
    <a href="{{ request.route_url('detail', entry_id=entry.id) }}"><h4>{{ entry.created.strftime('%b. %d, %Y') }}  ----  {{ entry.title }}</h4></a>
    <p class="entry_text" id={{ entry.id }} >{{ entry.text }}</p>
  {% else %}
    <div class="entry">
      <p><em>No entries here so far</em></p>
    </div>
  {% endfor %}
  </div>
  <footer>
  </a>
  <p>&copy; 2015 Tyler Peek.</p>
</footer>
</div>
</body>
</html>
